- get-flag.exe is given
    - get-flag.exe takes one input argument <username>
    - get-flag.exe returns the message "Hello <username>, the flag is letter X (uppercase).".
    - However, the message is encrypted with AES-ECB algorithm (HEX format)

- The flag is the letter X and it has a score of 8.
